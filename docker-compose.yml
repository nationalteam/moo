services:
  tabemap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tabemap-app
    environment:
      NODE_ENV: production
      PORT: 7751
    ports:
      - "7751:7751"
    restart: unless-stopped
